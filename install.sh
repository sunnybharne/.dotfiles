#!/bin/bash

# Create code dir
mkdir -p "$HOME/codeTestAgain"

# # Create the code directory if it doesn't exist
# if [ ! -d "$CODE_DIR" ]; then
#   echo "Creating directory: $CODE_DIR"
#   mkdir -p "$CODE_DIR"
# else
#   echo "Directory already exists: $CODE_DIR"
# fi
#
# # Define the repository URL and the target directory
# DOTFILES_REPO="https://github.com/yourusername/.dotfiles.git"  # Replace with your repository URL
# CLONE_DIR="$CODE_DIR/.dotfiles"
#
# curl -fsSL https://raw.githubusercontent.com/sunnybharne/.dotfiles/main/install.sh | bash
